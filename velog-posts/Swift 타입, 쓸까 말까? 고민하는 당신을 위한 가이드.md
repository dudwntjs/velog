<p>Swift에서는 변수나 상수를 선언할 때 <strong>타입을 직접 명시하지 않아도</strong> 되는 경우가 많습니다.
오늘은 Swift의 똑똑한 기능인 <strong>타입 추론</strong>과 명시적 선언인 <strong>타입 어노테이션</strong>에 대해 알아보겠습니다!</p>
<h1 id="타입-추론이란">타입 추론이란?</h1>
<p><strong>타입 추론</strong>은 변수나 상수를 초기화할 때 입력된 값을 분석해 컴파일러가 <strong>자동으로 타입을 결정하는 기능입니다.</strong></p>
<pre><code class="language-swift">var name = &quot;몬치치&quot;// String으로 추론
var year = 1974 // Int로 추론</code></pre>
<p>이렇게 값만 보고도 적절한 타입을 판단해주기 때문에, 코드가 훨씬 간결해진답니다. 
Swift의 '설계에 의한 안전성'을 보여주는 대표적인 기능이죠✨</p>
<h1 id="타입-어노테이션type-annotation이란">타입 어노테이션(Type annotation)이란?</h1>
<p>타입 어노테이션은 변수나 상수를 선언할 때 <strong>타입을 명시적으로 지정하는 문법</strong>입니다.</p>
<pre><code class="language-swift">var year : Int
var name : String
var distance : Double</code></pre>
<p>변수명 뒤에 <code>:</code>를 붙이고 타입을 작성합니다.</p>
<br />

<h2 id="타입-어노테이션이-왜-필요한가요">타입 어노테이션이 왜 필요한가요?</h2>
<p>대부분은 타입 추론이 다 해주는데, 반드시 써야 하는 <strong>두 가지 상황</strong>이 있습니다!</p>
<h3 id="1️⃣-선언과-초기화를-분리할-때">1️⃣ 선언과 초기화를 분리할 때</h3>
<p>Swift에서는 <strong>선언 시점에 타입이 반드시 결정되어야 합니다.</strong></p>
<p>만약 선언만 먼저 하고 나중에 값을 넣으려고 하면, 컴파일러는 참고할 값이 없어서 타입을 추론할 수 없게 됩니다<img alt="" src="https://velog.velcdn.com/images/dudwntjs/post/0aae08f1-1103-4203-b8d1-14c62be23172/image.svg" />
🤯</p>
<p><img alt="" src="https://velog.velcdn.com/images/dudwntjs/post/147066c7-757f-436f-83ff-44d4b46f9d4b/image.png" /></p>
<p>이 경우 타입 어노테이션이 빠져있다고 에러 메시지가 뜹니다😰</p>
<p><img alt="" src="https://velog.velcdn.com/images/dudwntjs/post/6b35a3a7-6315-4b9b-b9d1-cf5dea0a798b/image.png" /></p>
<blockquote>
<p>상수(<code>let</code>)는 기본적으로 선언과 동시에 초기화해야 하지만, <br /><strong>클래스나 구조체의 프로퍼티로 쓸 때는 선언만</strong> 할 수 있습니다. <br /> 단, 인스턴스가 만들어질 때 실행되는 <code>init</code> <strong>(생성자) 블록 안에서 반드시 초기값</strong>을 정해주어야 합니다.</p>
</blockquote>
<br />

<h3 id="2️⃣-타입-추론-결과가-원하는-타입이-아닐-때">2️⃣ 타입 추론 결과가 원하는 타입이 아닐 때</h3>
<p>🚨 타입 추론으로 결정된 타입이 <strong>의도와 다를 경우</strong>, 타입 어노테이션으로 직접 지정해야 합니다.</p>
<h4 id="예시-1-정수-대신-실수로-저장하고-싶을-때"><strong>예시 1: 정수 대신 실수로 저장하고 싶을 때</strong></h4>
<pre><code class="language-swift">var temper1 = 3 // Int
var temper2: Float = 3 // Float (3.0으로 저장)</code></pre>
<p>같은 값이라도 타입 어노테이션 여부에 따라 결과 타입이 달라집니다.</p>
<h4 id="예시-2-string-대신-character를-쓰고-싶을-때"><strong>예시 2: String 대신 Character를 쓰고 싶을 때</strong></h4>
<p>Swift에서는 문자열과 문자 모두 <strong>큰따옴표(&quot;)</strong> 를 사용하는데요!
그래서 한 글자일 경우 컴파일러는 기본적으로 <code>String</code>으로 추론합니다.</p>
<pre><code class="language-swift">var value = &quot;C&quot;// String</code></pre>
<p><code>Character</code>로 사용하고 싶다면 타입을 명시해야 합니다.</p>
<pre><code class="language-swift">var value: Character=&quot;C&quot;// Character</code></pre>
<br />

<h2 id="언제-타입-어노테이션을-쓰면-될까">언제 타입 어노테이션을 쓰면 될까?</h2>
<p>정리하자면, Swift에서는 다음 두 경우를 제외하고는 타입 어노테이션이 필수는 아닙니다!</p>
<ol>
<li><strong>선언과 초기화를 따로 할 때</strong></li>
<li><strong>원하는 타입을 직접 지정해야 할 때</strong></li>
</ol>
<p>그 외에는 Swift의 똑똑한 <strong>타입 추론</strong>에 맡겨도 충분합니다😎</p>
<p>물론 <strong>코드의 가독성</strong>을 위해, 혹은 본인의 코딩 스타일에 따라 타입을 명시해주는 것도 좋은 방법이에요.
정답은 없으니 상황에 맞춰 유연하게 사용해 보세요☺️</p>
<br />

<blockquote>
<ul>
<li>Swift는 타입 추론을 통해 대부분의 타입을 자동으로 결정한다</li>
</ul>
</blockquote>
<ul>
<li>타입 어노테이션은 <strong>필요할 때만</strong> 사용하면 된다</li>
<li>무조건 타입을 명시할 필요는 없으며, 이는 개인의 코딩 스타일 영역이다</li>
</ul>